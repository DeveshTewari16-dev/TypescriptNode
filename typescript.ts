const person: {
    name : string,
    age:number,
    hobbies: string[]
} = {
    name: 'Devesh',
    age:24,
    hobbies :['Basketball','Guitar']
}

console.log(person.name);

 //Object Type can be used in Typescript to basically let the type script know that how it should percieve the object
